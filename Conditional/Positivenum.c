/* Program to check whether the given number is positive  number or not */
/* Author:Sravanthi_Yachareni */
#include<stdio.h>
void main()
 {
 	int number;
 	printf("Enter a number");
 	scanf("%d",&number);
 	if(number>0)
 	{
 		printf("%d is a positive number.",number);
	 }
	 else
	 {
	 	printf("%d is not a positive number.",number);
	 }
  } 
